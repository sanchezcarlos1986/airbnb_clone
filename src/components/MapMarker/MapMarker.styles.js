import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  marker: {
    backgroundColor: "white",
    padding: 5,
    borderRadius: 10,
  },
  markerTitle: {
    fontWeight: "bold",
  },
});
